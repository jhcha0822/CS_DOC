feat: implement file attachments, soft delete, version management, and change history

FE
- PostEditorPage: file drag-and-drop attachments, UI overflow fixes, notice checkbox positioning, require explicit category selection
- PostDetailPage: attachment display with download links, separate view count increment, robust attachment JSON parsing
- PostListPage: attachment icon display, view count column
- PostVersionHistoryPage: new page with table/list view toggle, search by title/ID, change type filter (전체/생성/수정/삭제), version content preview
- SideNav: version history menu link
- api: attachment and version history APIs, deletePost, getAllChangeHistory

BE
- Post: deleted field (soft delete), currentVersionId field, markAsDeleted/restore methods
- PostVersion: new entity for version tracking (postId, versionNumber, contentMd, createdBy, createdAt)
- PostVersionRepository: version query methods, findAllByOrderByCreatedAtDesc
- PostService: savePostVersion (auto-version on content changes), getVersions/getVersion, delete (soft delete), listDeleted, getAllChangeHistory (unified history with changeType filter), deleted=false filtering in all queries
- PostController: POST /{id}/attachments, DELETE /{id}, GET /{id}/versions, GET /deleted, GET /changes/history
- PostRepository: deleted=true query methods, deleted=false added to existing queries
- AttachmentStorage: new service for file attachments
- PostSchemaMigration: deleted column, current_version_id column, post_version table
- ChangeHistoryItem: new DTO with PostCategory enum to String conversion fix
- PostVersionResponse: new DTO for version APIs
