feat: dynamic category management with hierarchical structure and categoryId-based filtering

FE
- CategoryManagePage: new category management page with CRUD, hierarchical move (up/down/in/out), inline editing, save/reset; buttons disabled by default, activate based on selection logic; input field flex layout
- SideNav: API-based dynamic category display (fetchCategories), "전체" link added, hierarchical structure with indentation, categoryId-based navigation
- PostListPage: categoryId-based filtering (replaces ApiCategory enum), API-based dynamic category loading, category filter buttons removed, page title shows selected category
- api: fetchPosts categoryId param; fetchCategories error handling; bulkUpdateCategories JSON error parsing; CategoryItem/CategoryBulkUpdateItem types
- router: /categories/manage route for CategoryManagePage

BE
- Category: new entity with parentId, depth, sortOrder; column names explicit (parent_id, depth, sort_order); index on sort_order
- CategoryController: list/create/update/bulkUpdate endpoints; CategoryResponse with parentLabel
- CategoryService: list with parentLabels map; create auto-generates code/depth/sortOrder; bulkUpdate with circular reference checks; getPostCategoriesByCategoryId for PostCategory mapping
- CategoryRepository: findByParentIdOrderBySortOrderAsc, findByParentIdIsNullOrderBySortOrderAsc
- CategoryDataLoader: auto-create/repair "신입 교육 자료" and children on startup; ensures data integrity
- PostService: categoryId param support; getPostCategoriesByCategoryId maps Category code to PostCategory; empty list returns empty PageResponse (prevents showing all posts for non-mappable categories)
- PostController: categoryId query param
- GlobalExceptionHandler: include error message in response for debugging
- migration_add_depth_column.sql: migration script for adding depth column
