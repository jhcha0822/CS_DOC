# TEMPORARY: Use this to auto-add missing columns
# After running once, change back to validate
spring:
  jpa:
    open-in-view: false
    properties:
      hibernate:
        ddl-auto: update  # TEMPORARY: Change back to 'validate' after first run
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug

app:
  storage:
    base-dir: ${CS_DOC_BASE_DIR:C:\\Users\\Kingdomcome\\Documents}
    md-root: ${app.storage.base-dir}/CS_DOC_DATA/md
    upload-dir: ${app.storage.base-dir}/CS_DOC_DATA/uploads
